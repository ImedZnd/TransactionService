server:
  port: 8081

spring:
  application:
    name: transaction-service
  sql:
    init:
      mode: always
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/postgres
    username: postgres
    password: example

logging:
  level:
    org:
      springframework:
        r2dbc: DEBUG
